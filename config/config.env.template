# sound2transcript configuration
# Copy to ~/sound2transcript/config/config.env and edit before use.
# Do NOT commit the actual config.env (it is gitignored).

# Audio device name as shown by:
#   ffmpeg -f avfoundation -list_devices true -i "" 2>&1
# After installing BlackHole, the name is exactly: BlackHole 2ch
BLACKHOLE_DEVICE_NAME="BlackHole 2ch"

# whisper-cli model path (see docs/MODELS.md for model comparison)
# Recommended: large-v3-turbo-q5_0 (fast + accurate multilingual, 547 MB)
# Alternative: ggml-medium.bin (balanced, 1.5 GB)
MODEL_PATH="$HOME/sound2transcript/models/ggml-large-v3-turbo-q5_0.bin"

# Language: 'auto' detects English and Portuguese automatically.
# Set to 'en' or 'pt' to force a specific language.
LANG="auto"

# Output format flags (1=enable, 0=disable)
OUTPUT_TXT="1"
OUTPUT_SRT="1"
OUTPUT_VTT="0"

# FFmpeg audio settings (do not change unless you know why)
SAMPLE_RATE="16000"
CHANNELS="1"

# Garbage collector retention
RECORDINGS_RETENTION_DAYS="3"
TRANSCRIPTS_RETENTION_DAYS="90"
RECORDINGS_MAX_GB="10"

# Threads for whisper-cli. See docs/MODELS.md for tuning by CPU.
# Apple Silicon: set to total cores minus 3 (e.g., M3 Pro 11 cores -> 8)
# Intel: set to core count (e.g., 4-core -> 4)
WHISPER_THREADS="8"

# Real-time silence monitor: seconds before first check, and between checks
SILENCE_CHECK_DELAY="10"
SILENCE_CHECK_INTERVAL="30"

# Silence detection threshold in dB. Recordings below this level are flagged as silent.
SILENCE_THRESHOLD_DB="-50"

# Keep WAV recordings after transcription (0=delete, 1=keep). Override per-session with --keep flag.
KEEP_RECORDINGS="0"

# Log level: info | warn | error
LOG_LEVEL="info"
